[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "musical-tuning"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
include = ["musical_tuning*"]
